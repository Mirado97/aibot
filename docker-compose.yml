services:
  aibot:
    build: .
    env_file: .env
    command: python app.py
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
